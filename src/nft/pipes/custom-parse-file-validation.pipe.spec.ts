import { CustomParseFileValidationPipe } from './custom-parse-file-validation.pipe';

describe('CustomParseFileValidationPipe', () => {
  it('should be defined', () => {
    expect(new CustomParseFileValidationPipe()).toBeDefined();
  });
});
