import { LimitValidationPipe } from './limit-validation.pipe';

describe('LimitValidationPipe', () => {
  it('should be defined', () => {
    expect(new LimitValidationPipe()).toBeDefined();
  });
});
