import { SymbolValidationPipe } from './symbol-validation.pipe';

describe('SymbolValidationPipe', () => {
  it('should be defined', () => {
    expect(new SymbolValidationPipe()).toBeDefined();
  });
});
